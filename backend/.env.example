# Lumina Backend Environment Variables

# Your Claude API key from console.anthropic.com
CLAUDE_API_KEY=sk-ant-api03-your-key-here

# MongoDB connection string for conversation history and user profiles
# Get a free database from mongodb.com/atlas
# Example: mongodb+srv://username:password@cluster.mongodb.net/lumina?retryWrites=true&w=majority
# Optional: If not set, the app will run without database persistence (memory only)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/lumina

# Comma-separated list of valid access codes for students
# Generate your own codes - make them memorable but not guessable
ACCESS_CODES=LUMINA2024,SPARK2024,LEARN2024

# Port (Railway will set this automatically)
PORT=3000
